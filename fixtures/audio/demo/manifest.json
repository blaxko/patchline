{
  "note": "All clips are real synthesized speech (Groq Orpheus, canopylabs/orpheus-v1-english, voice 'autumn', 16kHz PCM16LE mono, matching the Adapter's fixed session config). UPDATED 2026-09-10 (see TASKS.md's 'Live re-verification, round 3' addendum) with the full, real, causal demo loop: Call B (brk_known_failure.wav) shows the repair mechanism live on BRK-71Q9's Q/0 confusion, and captures its own real regression -- but that specific regression does NOT pass replay under cfg_keyterms_v3 (verified live: still mis-transcribes as BRK-7109, promotion correctly DENIED, POLICY_DENIED/TARGET_FAILS), because cfg_keyterms_v3's boosted terms only help the ID prefix vocabulary, not this suffix confusion. The regression that actually gets promoted comes from a SECOND repair cycle, on a different confusable id (ZXA-4B8K's B/V confusion, zxa_known_failure.wav) that cfg_keyterms_v3 genuinely does fix. That full chain -- live mishearing -> live repair -> real regression captured -> real replay pass (exactMatch:true) -> real promotion via POST /api/configs/:id/promote (not a DB bypass) -> a FRESH session (zxa_post_fix.wav) succeeding first-pass with no repair -- was run start to finish against the real, non-mocked API and is genuinely causal, not staged.",
  "clips": [
    {
      "id": "brk_known_failure",
      "file": "brk_known_failure.wav",
      "label": "Demo Clip: BRK-71Q9 (known failure)",
      "script": "Two turns in one file, matching a real caller's turn-taking: turn 1 says \"order BRK-7109\" (reliably transcribed by AssemblyAI exactly as spoken -- this is a genuinely misspoken/misremembered id from the caller, not a mis-hearing of the correct one); after a 1.5s pause, turn 2 says \"seven one Q nine\" (AssemblyAI transcribes this as the concatenated token \"71q9\", which the fixed repair parser now correctly reconstructs onto the known-good \"BRK-\" prefix as BRK-71Q9).",
      "role": "DEMO.md Call B -- verified live 5/5 times (see TASKS.md): entity_detected BRK-7109 -> action_blocked ENTITY_AMBIGUOUS -> repair_question asked -> entity_detected/resolved BRK-71Q9 -> action_allowed with the real order record. This is a real, reproducible live sequence, not merely what the mocked test suite scripts. Its own captured regression is NOT what later gets promoted (see the top-level note) -- kept in the demo purely to showcase the repair mechanism itself."
    },
    {
      "id": "zxa_known_failure",
      "file": "zxa_known_failure.wav",
      "label": "Demo Clip: ZXA-4B8K repair (regression source for promotion)",
      "script": "Two turns, same structure as brk_known_failure.wav: turn 1 says \"Checking on order ZXA-4B8K\" (the REAL seeded id); after a 1.5s pause, turn 2 repeats just the disputed suffix, \"four B eight K\" (AssemblyAI transcribes this as the concatenated token \"4b8k\", reconstructed onto the known-good \"ZXA-\" prefix).",
      "role": "DEMO.md's promotion step -- verified live: entity_detected ZXA-4V8K (mis-heard) -> action_blocked ENTITY_AMBIGUOUS -> repair_question asked -> entity_detected/resolved ZXA-4B8K -> action_allowed with the real order record -> a real regression captured. Replaying THIS regression against cfg_keyterms_v3 through the real /api/regressions/:id/replay endpoint passes (exactMatch:true) -- unlike Call B's regression -- so promoting cfg_keyterms_v3 against this regression via the real /api/configs/:id/promote endpoint genuinely succeeds (verified live, not a DB bypass)."
    },
    {
      "id": "zxa_post_fix",
      "file": "zxa_post_fix.wav",
      "label": "Demo Clip: ZXA-4B8K (post-fix, first-pass)",
      "script": "Checking on order ZXA-4B8K.",
      "role": "DEMO.md Call C -- a fresh session, same clip content as zxa_known_failure.wav's first turn, run AFTER cfg_keyterms_v3 is genuinely promoted. Verified live: entity_detected ZXA-4B8K -> action_allowed with the real order record, first-pass, zero repair_question messages. This is the real, causal payoff of the loop above, not an independently-staged claim."
    }
  ]
}
