{
  "note": "Both clips are real synthesized speech (Groq Orpheus, canopylabs/orpheus-v1-english, voice 'autumn', 16kHz PCM16LE mono, matching the Adapter's fixed session config), reading each clip's 'script' field aloud, padded with trailing silence to at least 6s. Streamed live through real AssemblyAI (see TASKS.md's Live API verification addendum), brk_known_failure.wav was actually mis-heard as 'BRK-7129' — a genuinely wrong id, but not the literal 'BRK-7109' this clip's 'role' field and DEMO.md narrate; that specific string is the mocked-AssemblyAI test suite's own scripted assumption (tests/integration/demoMode.test.ts), which stays deterministic by design regardless of what any real TTS/STT combination actually produces. Either way the mechanism is identical: an unverified wrong id blocks the tool call and triggers repair. Running the literal DEMO.md script against a live (non-mocked) AssemblyAI account may narrate a different specific mis-heard string than 'BRK-7109' for that reason.",
  "clips": [
    {
      "id": "brk_known_failure",
      "file": "brk_known_failure.wav",
      "label": "Demo Clip: BRK-71Q9 (known failure)",
      "script": "I need the status of order BRK-71Q9.",
      "role": "DEMO.md Call B — AssemblyAI mishears this as BRK-7109 under cfg_baseline_v1, triggering the repair flow."
    },
    {
      "id": "zxa_post_fix",
      "file": "zxa_post_fix.wav",
      "label": "Demo Clip: ZXA-4V8K (post-fix)",
      "script": "Checking on order ZXA-4V8K.",
      "role": "DEMO.md Call C — a DIFFERENT confusable id than Call B's, deliberately, to prove generalization once cfg_keyterms_v3 is promoted; should succeed first-pass under the promoted config."
    }
  ]
}
