# TASKS.md

Ordered checklist matching `PRD.md` §9 build steps. Update each item in place as work happens: what was implemented, tests run, test result, manual verification performed, unresolved issues. Do not delete failed-attempt history — append corrections instead, so this file stays an honest record.

- [ ] **Step 1 — Lock the product contract** — *Status: done.* Artifacts: `PRD.md` §0, `DECISIONS.md`. No code.
- [x] **Step 2 — Commerce sandbox**
  - Implemented: `prisma/schema.prisma` (`customers, orders, products, order_items, tool_call_audit`), `prisma/seed.ts`, `fixtures/commerce/seed.json` (6 customers incl. `Siobhan Mercer` and a digit/letter email trap on Renata Kowalski, 8 orders incl. the 3 confusable pairs from PRD §4, 10 SKUs incl. `WBH-100`/`WBH-100X`, refund eligibility mix of full/partial/ineligible). `services/commerce-sandbox/src/tools/*.ts` — typed, zod-validated functions for all 8 tools (`lookupOrder`, `lookupCustomer`, `checkTracking`, `lookupProduct`, `requestRefund`, `updateShippingAddress`, `createSupportCase`, `escalateToHuman`); Levenshtein-based close-match logic for order/tracking/SKU lookups; `tool_call_audit` row written for every mutating-tool call (`simulated: true` for `request_refund`/`update_shipping_address`, which never write to `orders`). Thin `POST /internal/tools/:name` Fastify wrapper (`src/server.ts`) for manual testing.
  - Tests: `services/commerce-sandbox/tests/*.test.ts` (vitest, real SQLite test DB via `tests/global-setup.ts` which runs `prisma db push` + the seed script against `prisma/test.db`) — 28 tests covering every seeded lookup success, all 3 confusable-pair structured failures with close-match candidates, invalid-format inputs (no throws), the SKU trailing-character pair, and refund eligibility (full/partial/ineligible/exceeds-eligible/not-found). Result: **28/28 passed**, 2026-09-09. `tsc --noEmit`: clean.
  - Manual verification: started `services/commerce-sandbox/src/server.ts` locally; `curl localhost:8081/internal/tools/lookup_order -d '{"order_id":"BRK-7109"}'` returned `{"found":false,"reason":"NOT_FOUND","close_matches":[{"order_id":"BRK-71Q9","distance":1}]}`, matching PRD §4's expected structure; `BRK-71Q9` returned the full order record. Confirmed 2026-09-09.
  - Unresolved: none.
- [x] **Step 3 — Live voice session**
  - Implemented: extended `prisma/schema.prisma` with `configs` (minimal Configuration Registry row — full promotion lifecycle deferred to Step 9, but the Adapter needs an active config's query params from the start), `sessions`, `utterances`, `audit_events`; seeded `fixtures/configs/seed.json` (`cfg_baseline_v1` active, `cfg_prompt_v2`/`cfg_keyterms_v3` draft, per PRD §6). `services/backend` (Fastify + `@fastify/websocket`): `src/realtime/token.ts` (AssemblyAI temp-token mint, see `DECISIONS.md` D9 — added because the exact endpoint shape wasn't in the original docs excerpt), `src/realtime/assemblyaiAdapter.ts` (WS client translating `Begin/Turn/Termination/Heartbeat`, builds query params from a config row), `src/realtime/audioBuffer.ts` (rolling in-memory per-session buffer, transient only per SECURITY.md), `src/realtime/gateway.ts` (browser-facing `/ws/session` WS route: session lifecycle per §2.1, writes `utterances` on `end_of_turn`, emits `session.started/transcript.partial/transcript.final/session.completed` to `audit_events`, reconnect-with-grace-window on AssemblyAI WS drop, `degraded` banner if grace elapses). `apps/web`: Next.js App Router `CallUI` component, `pcm-worklet.js` AudioWorklet capturing mic audio as ~100ms PCM16LE frames, `useVoiceSession` hook wiring mic capture + WS protocol + live partial/final transcript rendering.
  - Deviation from literal step text: `configs` needed to exist now (not just Step 9) since the Adapter reads `sessions.active_config_id` at connect time — noted here rather than in DECISIONS.md since it's a build-sequencing call, not an API-fact correction.
  - Tests: `services/backend/tests/integration/realtime.test.ts` against a mocked AssemblyAI WS server (`tests/mocks/assemblyaiWsServer.ts`, per TESTING.md's CI-determinism requirement) — asserts `utterances` rows match a scripted multi-turn call with correct `start_ms/end_ms/turn_order`; asserts the session reconnects and returns to `active` after a simulated AssemblyAI WS drop within the grace window; asserts a session that never connects at all reports `failed` once, without ever emitting a spurious `reconnecting` event. Found and fixed two real concurrency bugs in the process: (1) a dropped AssemblyAI socket firing both `error` and `close` could start two overlapping reconnect loops, duplicating transcripts — fixed with a `reconnecting` guard; (2) the same double-fire on the *initial* connect attempt caused a false `reconnecting` banner before the hard-failure path ran — fixed with a `hasBegun` guard so close-before-first-Begin is only ever handled as a connect failure. Result: **3/3 passed** (run 4× to confirm no flakiness), 2026-09-09. `tsc --noEmit` clean for both `services/backend` and `apps/web` (via `next build`).
  - Manual verification: started the real backend (`MOCK_ASSEMBLYAI=1`) and Next.js dev server together; confirmed `/ws/session` accepts a connection and correctly reports `{type:"failed"}` (no mock AssemblyAI running) without a false reconnect banner; confirmed the Next.js page and `/pcm-worklet.js` both serve over HTTP and `next build` typechecks clean. **Could not verify live mic capture end-to-end in a real browser** — this environment is headless with no browser/microphone available. The mocked-AssemblyAI integration test is the substitute verification for the transcript pipeline itself; the mic-capture/WS-wiring code path (`useVoiceSession.ts`, `pcm-worklet.js`) has not been exercised against a real browser and should be smoke-tested locally by a human before the live demo.
  - Unresolved: live-browser mic capture verification (see above) — flagged, not silently skipped.
- [x] **Step 4 — Entity evidence layer**
  - Implemented: `packages/schemas` — `entityTypes.ts` (the 10 critical types + rule-pass/LLM-fallback split per §5), `spokenNormalizer.ts` (NATO-alphabet/digit-word → character mapping, restricted to single-character literal tokens after a false-positive bug was found — see Tests), `extractors.ts` (deterministic regex + spoken-token-window extractors for `order_id`/`tracking_id`/`product_sku`/`coupon_code`/`email`/`phone_number`). `services/backend/src/reliability/extraction/` — `llmFallback.ts` (Groq `openai/gpt-oss-120b` JSON tool-call for `customer_name`/`shipping_address`/`refund_amount`, dependency-injectable for tests, timeout→skip per PRD's error-case spec, skips cleanly with no `GROQ_API_KEY` configured rather than throwing), `index.ts` (orchestrates rule pass → conditional LLM fallback → writes `entities` rows + `entity.detected` audit events). Added `entities` to `prisma/schema.prisma`. Wired into `services/backend/src/realtime/gateway.ts`: after each `end_of_turn` turn, extraction runs and any detected entities are pushed to the browser as `entity_detected` messages (sent after `final` so the frontend already has a line to attach them to). `apps/web`: `useVoiceSession`/`CallUI` now render entity markers inline under each transcript line.
  - Deviations (build-sequencing, not API-fact corrections — noted here per instructions): (1) PRD §5 gates the LLM fallback pass on "the Support Agent's intent classifier expects one of these types," but the Support Agent doesn't exist until a later step; substituted a simpler "rule pass found nothing AND turn has ≥3 words" heuristic, documented in `extraction/index.ts`. (2) The rule pass can emit more than one candidate entity type for the same span when a token's shape is structurally ambiguous (e.g. `BRK-7109` matches both `order_id` and the generic hyphenated-SKU pattern) — this is intentional per PRD §5 ("extraction never verifies"); real disambiguation is Step 5's job. Observed directly in manual verification below.
  - Tests: `packages/schemas/tests/extractors.test.ts` (17 tests) — every regex/normalizer against the PRD §4 confusable-ID fixtures (`BRK-71Q9`/`BRK-7109`, `ZXA-4B8K`/`ZXA-4V8K`, `NV-15O2`/`NV-1502`), the `WBH-100`/`WBH-100X` SKU pair, spoken-out order ids ("bravo romeo kilo seven one Q nine" → `BRK-71Q9`), spoken/direct email and phone extraction. Found and fixed a real false-positive bug during test-writing: the initial normalizer treated any short (≤4 char) English word as a literal ID character, so ordinary sentences ("my name is Siobhan Mercer...") produced fabricated order-id matches (`Siobhan` → `SIO-BHAN`); fixed by requiring the order-id "direct" pattern to include its dash and restricting spoken-token literal passthrough to single characters only — regression test added. `services/backend/tests/unit/extraction.test.ts` (3 tests) — asserts every written entity has `verification_state=unverified`, asserts no crash when `GROQ_API_KEY` is unset, asserts the injected-Groq-client path writes the LLM-fallback candidate correctly. Result: **20/20 passed**, 2026-09-09. `tsc --noEmit` clean for `packages/schemas`, `services/backend`; `next build` clean for `apps/web`.
  - Manual verification: sent a scripted mocked-AssemblyAI turn containing `order BRK-7109` through the real backend; confirmed an `entities` row was created with `raw_text`/`normalized_value` = `BRK-7109`, `entity_type=order_id`, `verification_state=unverified` (plus the expected secondary `product_sku`-shaped candidate from the same span, per the documented cross-classification behavior above); confirmed the browser WS stream receives `entity_detected` messages after the `final` transcript message, in an order the frontend can actually attach markers to. Could not verify the live-mic path in a real browser (same headless-environment limitation noted in Step 3).
  - Unresolved: none new (Step 3's live-browser mic verification gap still stands).
- [x] **Step 5 — Action gating**
  - Implemented: added `tool_calls`/`validation_results` to `prisma/schema.prisma`. `services/backend/src/reliability/validators/` — deterministic `validateOrderId`/`validateTrackingId`/`validateSku`/`validateCustomerIdentity`, each calling the real Commerce Sandbox tool functions (added `commerce-sandbox` as a workspace dependency of `services/backend`), fail-closed (`result:"unavailable"`) on any thrown error, never an LLM. `services/backend/src/reliability/gate/index.ts` — `evaluateToolCall()` implementing §7's pseudocode against the full §0 evidence-policy table for all 8 tools, including the harder cross-referencing ones (`request_refund`: order + caller-identity-matches-order's-customer + amount-matches-order-total; `update_shipping_address`: order + address `confirmed_by_caller`), writing `tool_calls`/`validation_results` rows and emitting `entity.validation_started/verified/rejected` + `action.allowed/action.blocked` per §8. Per the ownership table (Action Gate does not own tool execution), execution of allowed calls happens in the caller, not the gate.
  - Deviation (build-sequencing, not an API-fact correction): Step 5 presupposes a Support Agent already proposing tool calls, but no PRD step explicitly constructs that module and it doesn't exist yet. Added `services/backend/src/reliability/autoTrigger.ts`: a deterministic (non-LLM) stand-in that proposes the matching lookup tool call whenever an `order_id`/`tracking_id`/`product_sku` entity is detected, wired into the gateway after each turn's entity extraction. This is enough to exercise the full gate end-to-end live and satisfies the step's own manual-verification instruction; it is explicitly a placeholder, not the real Support Agent (Groq chat completions, full tool schema, conversational replies), which remains to be built when a later step needs it.
  - Tests: `services/backend/tests/integration/gate.test.ts` (20 tests) — one test per tool per PRD's own instruction ("this is the test the brief explicitly calls out"): correct evidence → allowed for all 8 tools; missing/ambiguous/conflicting evidence → blocked with the exact expected reason code (`LOW_EVIDENCE`, `ENTITY_NOT_FOUND`, `ENTITY_AMBIGUOUS`, `MISSING_CONFIRMATION`, `POLICY_DENIED` all exercised); confirms a blocked call's `tool_calls.final_args` is always `null`; confirms `request_refund`/`update_shipping_address` are always written with `simulated:true`. Result: **26/26 passed** (full suite, no regressions), 2026-09-09. `tsc --noEmit` clean.
  - Manual verification: ran the PRD's own demo scenario live (mocked AssemblyAI) — spoke "order BRK-7109" (the confusable fake id from §4); confirmed the Action Gate correctly returned `ENTITY_AMBIGUOUS` (not `ENTITY_NOT_FOUND`, since `BRK-7109` does have a close match — the PRD's own demo scenario text agrees, even though its Step 5 manual-verification line names `ENTITY_NOT_FOUND` as the example code) and the `tool_calls` row shows `gate_result=blocked`, `final_args=null`, `executed_at=null` — `lookup_order` never ran. The literal `ENTITY_NOT_FOUND` case is covered by the automated test suite (`ZZZ-0000`, no close matches) since no seeded fixture id matches "confusable but zero close matches" while also being spoken live.
  - Unresolved: the Support Agent stand-in (`autoTrigger.ts`) covers only read-only lookups by design (mutating tools have no natural "entity → propose" trigger without real conversational intent) — `request_refund`/`update_shipping_address`/`lookup_customer`/`create_support_case`/`escalate_to_human` are gate-tested but not yet reachable through a live call until the real Support Agent exists.
- [x] **Step 6 — Focused repair conversations**
  - Implemented: added `repair_events` to `prisma/schema.prisma`. `services/backend/src/tts/orpheusClient.ts` — real Groq Orpheus call (`POST https://api.groq.com/openai/v1/audio/speech`, model `canopylabs/orpheus-v1-english`, voice `autumn`, per DECISIONS.md D5), returns `null` on missing key/timeout/error so callers degrade to text-only rather than hard-failing (TESTING.md's TTS-failure row). `services/backend/src/reliability/repair/questionTemplates.ts` — question templates keyed by `(entity_type, gate_reason)` with a `spellOut()` helper for "I heard B-R-K-7-1-0-9..." phrasing, retry-prefixed on attempt 2. `services/backend/src/reliability/repair/index.ts` — `startRepair`/`handleRepairTurn`, repair budget of 2 attempts, module-level pending-repair map (one process, DECISIONS D6), reconstructs a full ID from a caller repeating only the disputed suffix ("seven one Q nine" + known-good prefix "BRK" → "BRK-71Q9", matching the PRD §0 demo scenario exactly) before re-validating once and transitioning the entity through `confirmed_by_caller → verified` or `→ escalated` per §2.2. Wired into `gateway.ts`: a pending repair claims the caller's next turn entirely (skipping normal extraction for that turn); wired into `autoTrigger.ts`: a blocked lookup starts a repair unless one is already pending for the session.
  - Deviation/limitation, documented rather than silently accepted: reconstruction-from-suffix is implemented for `order_id`/`tracking_id` only (the types reachable via Step 5's `autoTrigger` placeholder); `product_sku`/`refund_amount`/`shipping_address` templates exist but their repair-response parsing falls back to the full-value extractor only, consistent with the same Step 5 "no real Support Agent yet" limitation.
  - Bug found and fixed during manual verification (not by the automated tests, which used direct module calls and didn't exercise the live overlap): a single span like `BRK-7109` was being extracted as **both** `order_id` and `product_sku` (documented cross-classification behavior from Step 4/5), and `lookup_order`'s and `lookup_product`'s independent repairs both tried to claim the same one-pending-repair-per-session slot, silently clobbering each other. Fixed at the root: `packages/schemas` `extractRulePassEntities` now suppresses a `product_sku` candidate when the *exact same value* was already extracted as `order_id` in the same turn (added regression tests — the genuinely SKU-only case, e.g. `PWR-BANK-20K`, is unaffected). Also hardened `autoTrigger.ts` to never start a second repair while one is already pending, as defense in depth.
  - Tests: `services/backend/tests/integration/repair.test.ts` (2 tests) — full round trip exactly per PRD's own test spec ("blocked entity → repair question generated referencing the correct known-good prefix → simulated caller response → entity becomes confirmed_by_caller → gate re-evaluation now allowed"), and budget-exhaustion → escalation with the correct per-attempt `repair_events` rows (`unresolved` then `escalated`) and an `escalate_to_human` tool call. `packages/schemas/tests/extractors.test.ts` gained 2 regression tests for the cross-classification bug above. Result: **28/28 backend + 19/19 schemas passed**, 2026-09-10. Also fixed a pre-existing flaky test (the mocked-AssemblyAI reconnect test occasionally failed on a fixed port 9099 due to OS TIME_WAIT reuse) by having tests pick a fresh random port per test case — confirmed stable across 5+ consecutive full-suite runs afterward. `tsc --noEmit` clean; `next build` clean.
  - Manual verification: ran the PRD's own demo scenario live end-to-end (mocked AssemblyAI) — caller says "order BRK-7109", supervisor asks "I heard B-R-K-7-1-0-9 — could you repeat the last four characters of your order ID?", caller replies "seven one Q nine", `lookup_order` is then allowed and actually executes, returning the real `BRK-71Q9` record — full recovery in the same session with no reconnect, exactly matching the brief's headline demo. Could not verify actual TTS audio playback in a real browser (headless environment, and no `GROQ_API_KEY` configured here) — the text-only degraded path was what was exercised, which is itself one of the two required behaviors.
  - Unresolved: same Support Agent placeholder limitation as Step 5 (repair is only reachable for the entity types `autoTrigger` covers); live TTS audio path unverified for the reasons above.
- [x] **Step 7 — Automatic regression creation**
  - Implemented: added `regressions` to `prisma/schema.prisma`. `services/backend/src/reliability/regression/wav.ts` — a minimal 44-byte canonical WAV header writer plus `msToByteOffset()` (PCM16LE mono 16kHz, matching the Adapter's fixed session config, so an entity's `start_ms/end_ms` map directly to byte offsets in the rolling buffer). `services/backend/src/reliability/regression/index.ts` — `createRegressionFromRepair()`: pads the entity's audio range by 1.5s each side per §9 Step 7, slices it out of `audioBufferStore`, writes `{regression_id}.wav` under `AUDIO_STORAGE_DIR` (new env var), and writes the `regressions` row + `regression.created` event. Fails closed with a dedicated `AudioSegmentMissingError` (caught, logged, no row written) when the buffer no longer covers the requested range — never a regression without its audio, per the step's own error-case spec. Wired into the Repair Engine's `resolved` path (`repair_method = caller_confirmation`, matching `truth_source` per DECISIONS.md D8) via a new `createRegressionCase()` helper in `repair/index.ts`.
  - Deviation (same pattern as Steps 3–6, not an API-fact correction): "Frontend: Regression Lab list view gets the new open card" is Step 11's screen, which doesn't exist yet — this step delivers the backend creation half only, as its own goal statement says ("Regression Engine's creation half"); the card will render once Step 11 builds Regression Lab against this same `regressions` table.
  - Tests: `services/backend/tests/integration/regression.test.ts` (2 tests) — asserts a repair→regression round trip produces a valid, correctly-sized WAV file (`RIFF` header, byte length matching the padded ms range exactly) with the right `expected_value`/`observed_value`/`repair_method`; asserts the fail-closed path when no audio was ever buffered (repair and the underlying tool call still complete normally — missing audio blocks only regression *capture*, never the live call). Result: **30/30 passed** (full backend suite), 2026-09-10. `tsc --noEmit` clean.
  - Manual verification: ran the full live pipeline (mocked AssemblyAI + simulated mic audio frames sent over the real WS protocol) through the same "BRK-7109 → repair → BRK-71Q9" scenario; confirmed a `regressions` row was created with `audio_start_ms=0, audio_end_ms=3400` (the entity's actual `[1000,1900]` padded by 1500ms each side, clamped at 0), `repair_method=caller_confirmation`; confirmed the resulting `.wav` file at `audio_asset` has a valid `RIFF`/`WAVE`/`fmt `/`data` header (sample rate field decodes to 16000) and a byte length exactly matching `44 + (audio_end_ms - audio_start_ms) * 32`. Could not literally "play" the clip in a browser (headless environment) — verified via header/size validation instead, equivalent proof for a synthetic (non-perceptual) audio buffer.
  - Unresolved: none new.
- [x] **Step 8 — Regression Lab (replay)**
  - Implemented: new `packages/evaluation` (DB-free, pure functions per ARCHITECTURE.md's design goal) — `transcriptDelta.ts` (LCS word diff), `replayScoring.ts` (`scoreReplay()`: `exact_match`/`normalized_match`/`status`). Added `replay_runs` to `prisma/schema.prisma`. `services/backend/src/realtime/configMapping.ts` — extracted the config-row-to-adapter-config mapping (previously inlined in `gateway.ts`) into a shared helper reused by replay. `services/backend/src/reliability/replay/index.ts` — `runReplay()`: opens a fresh `AssemblyAIAdapter` per (regression, config) pair, streams the regression's stored WAV (minus its 44-byte header) in 100ms chunks at real-time pace per DECISIONS.md D1's pacing note, extracts the relevant entity type from the resulting transcript via the Step 4 extractor, scores it, and writes `replay_runs` + `regression.replay_started/completed` events; an individual config's connect failure is caught and scored as `fail` rather than aborting the batch. `services/backend/src/api/routes.ts` — the project's first REST endpoint, `POST /api/regressions/:id/replay {config_ids: []}`, registered in `server.ts` alongside the existing WS gateway.
  - Deviation (same pattern as Steps 3–7): "Frontend: Regression Lab detail view" is Step 11's screen; this step delivers the backend replay mechanism and API only.
  - Tests: `packages/evaluation/tests/replayScoring.test.ts` (6 tests) — exact vs. normalized match, transcript-delta collateral-damage detection, pure and DB-free. `services/backend/tests/integration/replay.test.ts` (1 test, matching PRD's own spec almost verbatim) — extended the mocked-AssemblyAI test server (`tests/mocks/assemblyaiWsServer.ts`) to accept a config-aware script function so a candidate's query params (presence of `keyterms_prompt`) determine its scripted transcript, the CI-safe substitute for "a candidate config that actually transcribes better" without a real AssemblyAI account; replays the same confusable-id regression against `cfg_baseline_v1` (asserts `fail`) and `cfg_keyterms_v3` (asserts `pass`) through the real HTTP route via Fastify's `.inject()`. Result: **6/6 evaluation + 31/31 backend passed** (confirmed stable across 8 consecutive full-suite runs), 2026-09-10. Also fixed a second pre-existing flaky-test cause found while re-running the suite repeatedly at its new, larger size: `realtime.test.ts`'s message-wait timeout (5s) was occasionally too tight under full-suite load; raised to 12s (and the file's overall `testTimeout` to 25s) — zero failures in 8 subsequent runs. `tsc --noEmit` clean.
  - Manual verification: ran the PRD's own example live — created a real regression via the Step 6/7 live-repair pipeline against the running backend, then `curl`'d the actual `POST /api/regressions/:id/replay` endpoint (not just an in-process test call) with a second, independent mock AssemblyAI server whose scripted transcript depended on the connecting config's `keyterms_prompt` query param. Result matched the brief's example table shape exactly: `cfg_baseline_v1 → fail (BRK-7109)`, `cfg_keyterms_v3 → pass (BRK-71Q9)`.
  - Unresolved: none new.
- [x] **Step 9 — Safe configuration promotion**
  - Implemented: `packages/evaluation/src/promotionGate.ts` — pure `evaluatePromotionGate()` + `computeP95()` implementing §2.6/§9 Step 9: target regression must pass, no previously-passing regression may flip to fail, suite p95 latency must stay within `LATENCY_REGRESSION_THRESHOLD` (new env var, default 1.25) of the active config's own p95; rejects outright (`NOT_FULLY_REPLAYED`) if any regression in the whole system hasn't been replayed against the candidate yet. Added `promotions` to `prisma/schema.prisma`. `services/backend/src/reliability/promotion/index.ts` — `promoteConfig()`/`rollbackConfig()`, each an atomic `prisma.$transaction` (flip both configs' `status` + write the immutable `promotions` snapshot together), emitting `config.promoted`/`config.rolled_back`. `services/backend/src/api/routes.ts` — `POST /api/configs/:id/promote` (`target_regression_id` optional, defaults to the first regression in the suite — the real UI, Step 11, always has one in context from the Regression Compare row clicked) and `POST /api/configs/:id/rollback`, both returning **422** (not 500) with the specific reason on policy denial, per the step's own error-case spec.
  - Bug found and fixed while writing the seed-reset flow (not the promotion logic itself): `prisma/seed.ts`'s `deleteMany()` order predated Steps 4–9's new tables and violated FK constraints on any re-seed once entities/tool_calls/regressions/etc. existed — fixed the delete order (children before parents); this was going to break `pnpm prisma db seed` for anyone re-running the documented bootstrap.
  - Tests: `packages/evaluation/tests/promotionGate.test.ts` (9 tests) — the exact 3 fixture scenarios PRD calls out (all-pass → eligible, one regression flips → rejected, latency over threshold → rejected) plus edge cases (exactly-at-threshold allowed, no active-config latency history skips the latency gate rather than blocking forever, not-fully-replayed rejected). `services/backend/tests/integration/promotion.test.ts` (3 tests) — full promote→rollback round trip against the real seeded `cfg_baseline_v1`/`cfg_keyterms_v3` (the PRD's own scenario), suite-regression rejection. Isolating these tests surfaced a real cross-file test-pollution bug: `regression.test.ts`/`replay.test.ts` left `Regression` rows in the shared test DB with no replay run for the candidate config, which the promotion gate's whole-system scan correctly (but unhelpfully, for other tests) flagged as `NOT_FULLY_REPLAYED` — fixed by having those files clean up their own rows. Result: **15/15 evaluation + 34/34 backend passed** (confirmed stable across 4 consecutive full-suite runs), 2026-09-10. `tsc --noEmit` clean.
  - Manual verification: ran the PRD's exact scenario against the live server — created a real regression via live repair, replayed it against both configs (`cfg_baseline_v1` fail, `cfg_keyterms_v3` pass) over the real HTTP endpoint, called `POST /api/configs/cfg_keyterms_v3/promote`: got the expected `POLICY_DENIED / NOT_FULLY_REPLAYED` against leftover regressions from earlier manual-verification sessions still in `dev.db` (correct, proves the whole-system scan is real) — cleaned those up and repromoted, which succeeded (`promotions` row, `cfg_keyterms_v3.status=active`, `cfg_baseline_v1.status=superseded`); then called rollback and confirmed full reversion.
  - Unresolved: none new.
- [x] **Step 10 — Close the learning loop live**
  - Implemented: no new backend module, per the step's own goal ("prove a promoted config changes live behavior") — this is a verification step confirming Step 3's "read `sessions.active_config_id` from the active `configs` row at session start" wiring actually closes the loop once Step 9 flips it. The one real code change: **fixed a genuine production bug found while writing this step's test**, in `services/backend/src/realtime/gateway.ts`'s `registerGateway()` — the browser-facing `socket.on("message", ...)` handler (which feeds both the AssemblyAI adapter and the regression audio buffer) was registered only *after* several `await`s (session insert, `adapter.connect()`), so any audio frames a real client sent the instant its WebSocket opened — exactly what the Call UI's `useVoiceSession.ts` does — could be silently dropped before that handler ever existed. Fixed by registering the message listener synchronously at connection time, buffering frames into an array until the per-session setup completes, then draining that buffer into the real handler. This bug would have intermittently broken regression-audio capture (and even occasionally dropped the caller's first words) in real use, not just in this test.
  - Tests: `services/backend/tests/integration/demoCallC.test.ts` — scripts the full DEMO.md Call B→C flow against the real running server (real Fastify HTTP + WS stack, mocked AssemblyAI, real binary audio frames over the real WS protocol — not direct in-process function calls): production failure on `BRK-7109` → live repair → regression created → replayed against `cfg_keyterms_v3` → promoted → a **new** session on a **different** confusable id (`ZXA-4B8K`, matching DEMO.md's explicit "a different confusable ID than Call B's, deliberately, to prove generalization rather than clip-memorization") succeeds first-pass with zero `repair_question` messages, and the new session's stored `active_config_id` is confirmed to be the newly-promoted config. Iterating on this test is what surfaced the early-audio-frame race above, and separately confirmed the promotion gate's latency check behaves exactly as documented (skipped when the active config has no replay history — noted inline in the test, since two near-instant mock-server replies otherwise produce noise-dominated latency numbers at the gate's threshold). Result: **stable across 6 consecutive runs of this test + 5 consecutive full-suite runs (35/35)**, 2026-09-10. `tsc --noEmit` clean.
  - Manual verification: the integration test above *is* the live verification for this step — it exercises the real server process (HTTP `POST` routes, real WS upgrade, real Fastify router), not test-internal shortcuts, which is the same standard used for every other step's "manual verification" in this build given no browser/mic is available in this environment.
  - Unresolved: none new.
- [x] **Step 11 — Operator dashboard**
  - Implemented: **Backend** — `services/backend/src/metrics/overview.ts` (`computeMetricsOverview()`: all 10 §8 core metrics computed from `entities`/`tool_calls`/`repair_events`/`regressions`/`replay_runs`, never hardcoded), `src/api/dashboardRoutes.ts` (`GET /api/metrics/overview`, `/api/sessions`, `/api/sessions/:id`, `/api/regressions`, `/api/regressions/:id`, `/api/configs`, plus `GET /api/regressions/:id/audio` to serve the captured clip — not yet behind operator auth, Step 13's job), `src/realtime/dashboardChannel.ts` (`GET /ws/dashboard`, mirroring the event catalog 1:1 via a new `auditEventBus` EventEmitter wired into `writeAuditEvent()`). Two real spec gaps found and closed while wiring this: (1) `promoteConfig()` never transitioned a regression to `closed` on a passing target, per §2.6 — added; (2) `runReplay()` never transitioned a config `draft → tested` per §2.6 — added. **Frontend** — `apps/web/app/(dashboard)/*`: Reliability Overview (`/`, metric tiles), Live Sessions (`/sessions`), Session Detail (`/sessions/[id]`, reuses `EvidenceTimeline`), Regression Lab (`/regressions`, `/regressions/[id]` — config-picker checkboxes, Replay button, reuses `RegressionCompare`, inline `<audio>` playback, Promote button wired to the real gate with the denial reason surfaced on 422), Configurations (`/configs`, Rollback button). `components/dashboard/EvidenceTimeline.tsx` and `RegressionCompare.tsx` are the two signature components, each usable standalone. `lib/useDashboardLive.ts` subscribes to `/ws/dashboard` and falls back to 3s polling on drop, per the step's own error-case spec. Moved the existing Call UI from `/` to `/call` since the dashboard now owns the root route (both can't own `/`); linked from the dashboard nav.
  - Tests: `packages/evaluation`/`services/backend` unchanged coverage plus `services/backend/tests/integration/dashboardRoutes.test.ts` (4 tests) — all 10 metric keys present, configs/sessions/regressions list+detail round-trip real rows including 404s. `apps/web` gained a test runner for the first time (`vitest` + `@testing-library/react` + `jsdom`) — `components/dashboard/__tests__/EvidenceTimeline.test.tsx` and `RegressionCompare.test.tsx` (5 tests total), against fixture event/replay sequences shaped exactly like PRD §11's own examples, per the step's explicit test requirement. Result: **43/43 backend + 5/5 web component tests passed**, 2026-09-10 (backend suite confirmed stable across 3 consecutive full runs). `tsc --noEmit` clean for backend; `next build` clean for web.
  - Manual verification: walked all 5 screens' underlying data live — created a real session/regression via the mocked pipeline, then confirmed `GET /api/metrics/overview` reflected it exactly (`repair_rate: 1`, `unsafe_action_prevention_count: 1`, etc., all computed not hardcoded); confirmed `GET /api/sessions/:id`'s event list is the identical 12-event sequence the PRD's own Evidence Timeline example describes; confirmed the audio endpoint serves a valid WAV; confirmed `/ws/dashboard` pushes the full live event sequence in real time during a new call (captured and inspected verbatim); confirmed all 5 page routes (`/`, `/sessions`, `/regressions`, `/configs`, `/call`) serve 200 from a real `next dev` server.
  - Unresolved: the Regression Compare's "eligible" config badge (§11: "Promote button appearing next to any PASS row whose config is eligible") is simplified to "any PASS row whose config isn't already active" — the backend's real promotion gate is still the sole enforcer (a non-eligible promote attempt correctly 422s with the reason shown), so nothing unsafe is exposed, but the pre-emptive UI hint is coarser than the literal spec.
- [x] **Step 12 — Observability and evidence (coverage audit)**
  - Implemented: `tests/integration/event_coverage.test.ts` (root-level, per ARCHITECTURE.md's layout) — walks every non-test `.ts` file under `services/backend/src` and asserts each of the 17 event types in PRD.md §8's catalog appears as a string literal at least once (an emission site), plus a second check that `events.ts`'s `EventType` union itself still matches the catalog exactly (catches silent drift if either list is edited without the other). Added a root `vitest.config.ts` (`tests/**/*.test.ts`) since this is the first root-level (non-package) suite, and wired `pnpm test` to run it after all per-package suites.
  - Tests: **18/18 passed** (17 event types + 1 catalog-match check), 2026-09-10. Ran alongside the full repo's test suite for a whole-project confirmation: evaluation 15, schemas 19, web 5, commerce-sandbox 28, backend 39, this root suite 18 — **124 tests total, all green**.
  - Manual verification: cross-referenced 3 events from a real captured `/ws/dashboard` sequence (Step 11's live verification) against dashboard-visible behavior: `action.blocked` → Session Detail's Evidence Timeline row "lookup_order blocked (reason: ENTITY_AMBIGUOUS)" and the Call UI's live tool-activity feed; `regression.created` → a new row appears in Regression Lab (`GET /api/regressions`); `entity.verified` → Session Detail's entity badge shows `verified` and the matching Evidence Timeline row. All three trace to a real, currently-buildable dashboard screen — no orphaned event type exists that the UI can't explain.
  - Unresolved: no lint rule enforcing "new state transitions must emit an event" was added (PRD.md mentions this as an optional code-review checklist item, not a hard requirement) — relying on this test file plus code review for now.
- [x] **Step 13 — Privacy and security controls**
  - Implemented: `services/backend/src/auth/session.ts` — HMAC-SHA256 signed session tokens (`createSessionToken`/`verifySessionToken`, 12h TTL, timing-safe comparison) and a manual cookie parser (no new cookie-parsing dependency). `src/auth/routes.ts` — `POST /api/auth/login`/`/api/auth/logout` plus a global `onRequest` hook gating every `/api/*` route and `/ws/dashboard` (never the caller-facing `/ws/session` — that's the public voice-call endpoint, not an operator control). Auth is a no-op when `OPERATOR_PASSWORD` is unset (documented dev/test convenience, "no default password shipped" per SECURITY.md) and always off under `NODE_ENV=test` specifically because Prisma Client auto-loads `.env`, which could otherwise leak a real `OPERATOR_PASSWORD` into test runs and make them environment-dependent (discovered this while the auth tests first failed against a real, already-populated local `.env`). `server.ts` refuses to boot when `NODE_ENV=production` and either `OPERATOR_PASSWORD` or `SESSION_SECRET` is missing. `src/reliability/regression/retention.ts` — `cleanupExpiredAudio()`: deletes only `closed` regressions' `.wav` files past `AUDIO_RETENTION_DAYS` (open/replaying/replayed/reopened are exempt regardless of age, per SECURITY.md), never the DB row; scheduled daily in the real boot path. Added `@fastify/cors` (credentialed cross-origin requests need an explicit allowed origin, not `*`) and a new `GET /api/regressions/:id/audio` consumer-side change: `apps/web` gained `/login`, an `AuthGate` wrapping the dashboard layout (redirects to `/login` on a 401), and `credentials: "include"` on every dashboard fetch.
  - Tests: `services/backend/tests/unit/session.test.ts` (9 tests) — valid/wrong-secret/tampered-payload/missing/malformed/expired tokens, cookie parsing. `tests/integration/auth.test.ts` (5 tests, real HTTP through the actual `onRequest` hook — deliberately overrides `NODE_ENV` for just this file, restored in `afterAll`, safe because `fileParallelism:false` keeps test files sequential not concurrent) — no cookie → 401, invalid cookie → 401, wrong password → 401, correct login → cookie → 200, `/ws/session` never gated. `tests/unit/retention.test.ts` (3 tests, fixture clock via an explicit `now` param) — closed+expired deletes the file but not the row, open+very-old is exempt, closed+recent is exempt. Result: **56/56 backend passed** (confirmed stable across 3 consecutive full runs), 2026-09-10. `tsc --noEmit` clean; `next build` clean.
  - Manual verification: started the real server with `NODE_ENV=production` and both required env vars set (confirming the boot-time check itself doesn't block a correctly-configured start); confirmed `GET /api/configs` → 401 with no cookie, `POST /api/auth/login` with a wrong password → 401, with the correct password → sets a working cookie that then makes `GET /api/configs` → 200; confirmed a raw WS upgrade to `/ws/dashboard` → 401 without auth, while the same upgrade to `/ws/session` proceeds (not gated); confirmed `.env` is git-ignored (`git check-ignore -v .env`) and `.env.example` contains no real values (all blank/placeholder). Used a synthetic password for these live checks rather than the real value already present in the local `.env`, to avoid ever exposing that value.
  - Unresolved: `REDACT_TRANSCRIPT_PII` remains the documented no-op flag SECURITY.md always intended it to be — not activated, since the demo data is synthetic and there's nothing to redact from in this build.
- [x] **Step 14 — Adversarial tests**
  - Implemented: `fixtures/audio/adversarial/manifest.json` — 10 labeled fixtures covering every category PRD §9 Step 14 lists (fast speech, background noise, mid-utterance spelling correction, O/0 confusion, B/V confusion, repeated value, interruption/barge-in, caller changes mind mid-entity, uncommon name spelling, 18/80 amount ambiguity), each with `{clip, entity_type, expected_value, expected_gate_reason, expected_tool_called}` per the step's own manifest shape. `tests/regression/adversarial.test.ts` (new root-level suite) runs every fixture through the real pipeline (`extractEntitiesForUtterance` → `evaluateToolCall`, and the Repair Engine's `resolveEntityOrConflict` where relevant) and asserts the **business outcome** (tool allowed/blocked, exact gate reason) per TESTING.md's governing rule, never just transcript text.
  - **Documented limitation**: no `GROQ_API_KEY` is configured in this environment, so no real synthetic speech audio was generated — each fixture's `transcript` field stands in for what AssemblyAI would have returned for that spoken condition, using the exact same mocked-transcript technique every other test in this repo already relies on (TESTING.md's "CI determinism" section explicitly sanctions a mocked AssemblyAI server for this reason). PRD.md §9 Step 14 itself leaves the exact synthesis method to the implementer ("any reasonable synthetic method satisfies this step"); this build could not clear the "real audio" bar specifically because of the missing API key, not by choice. The `clip` field in the manifest names where a real `.wav` would live once one is generated.
  - **Real gap found and fixed while building this step**: `VALUE_CONFLICT` was defined in the `GateReason` type since Step 5 but **no code path ever returned it** — a caller stating two different values for the same slot in one turn (the mid-utterance-correction and changed-mind categories this step explicitly requires) was silently resolved by just picking whichever entity happened to be created last, never flagged as a conflict. Fixed in `services/backend/src/reliability/gate/index.ts`: a new `resolveEntityOrConflict()` checks whether multiple *distinct* values were extracted from the *same utterance* for a slot and returns `VALUE_CONFLICT` before any validator runs, wired into the `lookup_order`/`check_tracking`/`lookup_product` path. Repeating the *same* value (the `repeated_value` fixture) is correctly distinguished from this and still allowed normally.
  - Tests: **11/11 adversarial fixtures passed** (10 category fixtures + 1 manifest-completeness check), 2026-09-10. Also fixed a real cross-package test-runner bug discovered while wiring this: adding a root `vitest.config.ts` caused Vitest to silently shadow every workspace package's own `vitest.config.ts` when running `pnpm -r run test` (schemas/evaluation reported "no tests" and evaluation's global-setup path resolved against the wrong directory) — renamed to `vitest.root.config.ts` and invoked explicitly via `--config`, confirmed all 6 packages' suites run independently again. Full repo `pnpm test`: **153 tests, all green**, confirmed stable across 2 consecutive runs; `pnpm run typecheck` clean across all 5 TS packages.
  - Manual verification: spot-checked 3 fixtures (`o_zero_confusion`, `spelling_correction_mid_utterance`, `b_v_confusion`) live against the real running server (mocked AssemblyAI, real WS protocol) rather than literally "playing the clip into the mic" — no real audio exists per the limitation above. All three produced the correct repair question text confirming the pipeline recognized the adversarial condition (`"I heard N-V-1-5-0-2..."`, `"I heard Z-X-A-4-V-8-K..."`, and the generic fallback question for the `VALUE_CONFLICT` case, which has no ID-specific template). The automated suite above is the authoritative, deterministic verification; this live run is corroborating evidence only.
  - Unresolved: real synthesized adversarial audio (`.wav` files) do not exist in this build — flagged, not silently skipped. A human with a configured `GROQ_API_KEY` (or any TTS source) should generate them from the manifest's `transcript` fields to fully close this out.
- [x] **Step 15 — Deterministic judge demo**
  - Implemented: `services/backend/src/realtime/wavStreamer.ts` — `streamWavFile()`: streams a stored WAV's PCM through an already-connected `AssemblyAIAdapter` at real-time pace (100ms chunks), the exact same pacing Step 8's replay uses. Extended `registerGateway()` in `gateway.ts`: `GET /ws/session?clip=<id>` selects `prerecorded_clip` mode — the browser sends no audio at all; instead the backend streams the demo clip itself through the same Adapter code path as live mic input (byte-identical pipeline downstream, only the audio source differs, per the step's own goal statement), while still feeding the same bytes into `audioBufferStore` so Step 7's regression capture works unchanged. `services/backend/src/demo/clips.ts` (reads `fixtures/audio/demo/manifest.json`) and `src/demo/reset.ts` (`resetDemo()`: force-completes any still-active session, deletes every regression/replay_run/promotion/repair_event/tool_call/entity/utterance/session/audit_event in FK-safe order, deletes each closed-out regression's audio file, resets `cfg_baseline_v1` to `active` and every other config to `draft`). New routes: `POST /api/demo/reset`, `GET /api/demo/clips`. `fixtures/audio/demo/` — two demo clips (`brk_known_failure.wav`, `zxa_post_fix.wav`) matching DEMO.md's Call B/C exactly. **Frontend**: `apps/web/components/CallUI.tsx` gained a clip picker (Live Mic / two demo clips) wired through `useVoiceSession.ts`'s `start(clipId?)`, which skips mic capture/AudioWorklet setup entirely in clip mode; the dashboard `Nav` gained a "Reset Demo" button (confirms, calls the reset endpoint, reloads).
  - Real bug found and fixed while writing this step's test: `gateway.ts`'s `completeSession()` called `prisma.session.update()` unconditionally on WS close, which throws `P2025` (unhandled promise rejection) if the session row was already deleted — exactly what happens when an operator clicks **Reset Demo** the instant a caller's browser tab closes, a genuine real-world race this step's own error case gestures at ("reset called mid-active-session"). Fixed: catches `P2025` specifically and treats it as "nothing left to complete," still cleaning up in-memory session state (audio buffer, pending repair, adapter socket) rather than throwing.
  - **Documented limitation** (same root cause as Step 14): the two demo clip `.wav` files are silent placeholders, not real synthesized speech — no `GROQ_API_KEY` is configured in this build environment to render TTS audio. The manifest's `script` field states exactly what a human recording (or real TTS render) would need to say for the demo to work against a live AssemblyAI account; the streaming *mechanism* itself is fully implemented and tested end-to-end against these placeholder files, which is what this step's own goal is actually about ("prerecorded_clip sessions stream a WAV file through the same Adapter code path... at real-time pace").
  - Tests: `services/backend/tests/integration/demoMode.test.ts` (2 tests) — streams the known-failure clip through the real `?clip=` WS path, confirms `session.mode=prerecorded_clip` and the identical repair→regression outcome as a live call (Step 10's own test), calls the real `POST /api/demo/reset`, confirms sessions/regressions are actually gone and `cfg_baseline_v1` is `active` again, then re-runs the identical script and confirms an identical outcome — literally "run twice, identical outcome" as the step's own manual-verification line asks, just automated. Plus a clips-listing endpoint test. Result: **58/58 backend passed** (confirmed stable across 5 consecutive `demoMode.test.ts`-only runs plus 2 consecutive full-suite runs after the `completeSession` race fix), **154 tests across the whole repo, all green**, 2026-09-10. `tsc --noEmit` clean; `next build` clean.
  - Manual verification: ran the literal documented bootstrap from a cold start — `pnpm prisma migrate dev`, `pnpm prisma db seed`, `pnpm dev` — all 3 services (`commerce-sandbox`:8081, `backend`:8080, `web`:3000) started cleanly with zero errors, confirmed by hitting the frontend (200) and an authenticated-gated backend route (401 with no cookie, correctly enforcing the real `OPERATOR_PASSWORD` already set in this environment's `.env`). Separately ran DEMO.md's Call B script twice against the real running server with a real `POST /api/demo/reset` in between (auth disabled for this specific check, to avoid exercising the real operator credential): first run created a regression and completed; reset returned 200 and the first session became unreachable (404); second run reproduced the byte-for-byte identical outcome (`BRK-71Q9`/`BRK-7109`, `caller_confirmation`, exactly 1 regression row) — confirmed identical outcomes across two consecutive runs.
  - **All 15 PRD.md §9 build steps and the §10 Acceptance Criteria are now complete** (§10 checked off in `PRD.md` directly, with two items carrying honest caveats: no live-LLM Support Agent module — see Step 5's own documented simplification — and no browser-based Playwright e2e, substituted with full-stack integration tests since no browser is available in this environment).
  - Unresolved: real synthesized demo-clip audio does not exist (same as Step 14 — flagged, not silently skipped); a human with a configured `GROQ_API_KEY` or other TTS source should record the two scripts in `fixtures/audio/demo/manifest.json` to fully close this out for a live-AssemblyAI judge run.

Each unchecked item, when completed, should be edited in place to include:

```
- [x] **Step N — Name**
  - Implemented: <files/modules>
  - Tests: <what was added> — Result: <pass/fail, date>
  - Manual verification: <what was checked, outcome>
  - Unresolved: <none, or specific open issue>
```

## Planning audit (performed before implementation begins, per operating instructions)

1. **Every hackathon requirement covered?** Yes — `PRD.md` §10 Acceptance Criteria maps 1:1 to brief §19.
2. **AssemblyAI load-bearing?** Yes — Universal-Streaming is the source of every entity, every gate decision, and the entire replay/promotion mechanism (`PRD.md` §1, §8, §9 Steps 3/8). Nothing routes around it.
3. **One dominant mechanism?** Yes — `live speech → entity → validate → gate → repair → regression → replay → promote → improved call`, stated once in the brief's opening and preserved verbatim as the spine of `PRD.md` §9.
4. **No critical action relies on unverified speech?** Yes — Action Gate (`PRD.md` §7) is the sole path to tool execution; enforced by tests in `TESTING.md`.
5. **Regression truth has provenance?** Yes — `regressions.repair_method` enum has no LLM-guess member (`DECISIONS.md` D8).
6. **Configuration promotion evidence-backed?** Yes — promotion gate requires full-suite non-regression + latency threshold (`PRD.md` §9 Step 9).
7. **Demo reproducible?** Yes — `prerecorded_clip` mode + `/api/demo/reset` (`PRD.md` §9 Step 15, `DEMO.md`).
8. **Every UI state maps to real backend data?** Yes — Dashboard is read-only over `audit_events`/primary tables, no hardcoded metrics (`PRD.md` §8, §11).
9. **Every major dependency has a failure path?** Yes — `TESTING.md` failure-injection table covers AssemblyAI, Groq (LLM + TTS), validators/DB, audio storage, replay.
10. **Understandable/runnable from the public repo?** Yes — `README.md` + `ARCHITECTURE.md` bootstrap instructions, `.env.example`, seeded fixtures, no external services beyond AssemblyAI/Groq API keys.

Audit result: **plan is internally consistent.** Implementation may proceed step by step per `PRD.md` §9, in order, updating this file after each step.
